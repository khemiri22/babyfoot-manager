---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ .Release.Name }}-app
  ports:
  - port: {{ .Values.port }}
    targetPort: {{ .Values.port }}
    nodePort: {{ .Values.nodePort }}
  type: NodePort
